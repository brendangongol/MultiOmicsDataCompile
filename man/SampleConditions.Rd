% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-conditions.R
\name{SampleConditions}
\alias{SampleConditions}
\title{Derive sample conditions from a dataset manifest}
\usage{
SampleConditions(DBPath, overview, subsetRaw = FALSE)
}
\arguments{
\item{DBPath}{A path containing the raw-expression RDS files written by
\code{\link[=GEOCompile]{GEOCompile()}}.}

\item{overview}{A dataset manifest containing \code{ID}, \code{ComparisonVector}, and
\code{FCColumnNames} columns, and optionally a logical \code{GEO2R} column.}

\item{subsetRaw}{A logical value indicating whether the stored raw data were
subset to the samples used in each comparison.}
}
\value{
A data table with \code{dataset}, \code{rawcolumnnames}, \code{condition},
\code{comparison}, and \code{comparison_label} columns, containing one row per
sample.
}
\description{
Reads the raw-expression files written by \code{\link[=GEOCompile]{GEOCompile()}} and assigns each
sample the group name recorded in the manifest's \code{ComparisonVector} and
\code{FCColumnNames} columns. Conditions are therefore taken from an explicit,
user-authored comparison code; they are never inferred from sample order.
}
\details{
A comparison code positions one character per sample of the stored raw
matrix, so the assignment is a direct lookup rather than a guess. When a
dataset appears in several manifest rows, the first comparison that includes
a sample determines its condition. Samples excluded (\code{X}) from every
comparison are labelled \code{"Unassigned"} so that downstream objects still carry
a condition for each column without implying group membership.
}
\examples{
directory <- tempfile()
dir.create(directory)
raw <- data.frame(
  ENTREZID = c("1", "2"), SYMBOL = c("A", "B"),
  GSE1_S1 = c(5, 3), GSE1_S2 = c(6, 4),
  GSE1_S3 = c(9, 1), GSE1_S4 = c(8, 2)
)
saveRDS(raw, file.path(directory, "GSE1_Raw.rds"))
manifest <- data.frame(
  ID = "GSE1",
  ComparisonVector = "0011",
  FCColumnNames = "NASH-Ctrl_logFC,NASH-Ctrl_Pvalue,NASH-Ctrl_AdjPValue",
  stringsAsFactors = FALSE
)
SampleConditions(directory, manifest)
}
\seealso{
Other transcriptomics functions:
\code{\link[=DESEGenerate]{DESEGenerate()}},
\code{\link[=ExternalDataHarmonize]{ExternalDataHarmonize()}},
\code{\link[=GEO2RDirectionCheck]{GEO2RDirectionCheck()}},
\code{\link[=GEOCompile]{GEOCompile()}},
\code{\link[=GenerateRawSE]{GenerateRawSE()}},
\code{\link[=MetDataCompile]{MetDataCompile()}},
\code{\link[=PlatformAnnotationLoad]{PlatformAnnotationLoad()}},
\code{\link[=RNAseqQC]{RNAseqQC()}},
\code{\link[=RawDataCompile]{RawDataCompile()}}
}
\concept{transcriptomics functions}
