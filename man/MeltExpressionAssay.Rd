% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression-reshape.R
\name{MeltExpressionAssay}
\alias{MeltExpressionAssay}
\title{Reshape an experiment assay to long format}
\usage{
MeltExpressionAssay(
  se,
  assay_name = 1L,
  row_columns = NULL,
  col_columns = NULL
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object.}

\item{assay_name}{The name or index of the assay to reshape. When a name is
supplied it is also used as the value column name.}

\item{row_columns}{An optional character vector of \code{rowData} columns to
attach. Columns that are absent are ignored.}

\item{col_columns}{An optional character vector of \code{colData} columns to
attach. Columns that are absent are ignored.}
}
\value{
A data frame with \code{feature}, \code{sample}, the requested annotation
columns, and one value column.
}
\description{
Converts one assay of a \code{\link[SummarizedExperiment:SummarizedExperiment]{SummarizedExperiment::SummarizedExperiment()}} to a
long data frame with one row per feature and sample, optionally carrying
selected \code{rowData} and \code{colData} columns. This replaces the equivalent
helpers in single-cell packages so that the explorer does not need an extra
dependency, and so that the reshaping used by the application can be tested.
}
\examples{
counts <- matrix(
  1:6, nrow = 3,
  dimnames = list(paste0("gene", 1:3), c("s1", "s2"))
)
se <- SummarizedExperiment::SummarizedExperiment(
  assays = list(Expression = counts),
  rowData = data.frame(SYMBOL = c("A", "B", "C"), row.names = rownames(counts)),
  colData = data.frame(condition = c("ctrl", "tx"), row.names = colnames(counts))
)
head(MeltExpressionAssay(se, "Expression", "SYMBOL", "condition"))
}
\seealso{
Other exploration functions:
\code{\link[=VolcanoPlot]{VolcanoPlot()}},
\code{\link[=densityPlotFromList]{densityPlotFromList()}},
\code{\link[=launchMultiOmicsExplorer]{launchMultiOmicsExplorer()}},
\code{\link[=plot_volcano2]{plot_volcano2()}}
}
\concept{exploration functions}
